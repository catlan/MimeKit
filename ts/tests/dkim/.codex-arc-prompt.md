Port these two C# NUnit test files to TypeScript vitest tests, 1:1, ZERO dropped test cases:
- SOURCE: UnitTests/Cryptography/ArcSignerTests.cs  ->  DEST: ts/tests/dkim/arc-signer.test.ts
- SOURCE: UnitTests/Cryptography/ArcVerifierTests.cs  ->  DEST: ts/tests/dkim/arc-verifier.test.ts

The DKIM/ARC TypeScript subsystem is ALREADY IMPLEMENTED under ts/src/dkim/. Do NOT modify anything in ts/src/. Only create the two test files. Study the existing ts/tests/dkim/smoke.test.ts and ts/tests/dkim/arc-smoke.test.ts (already passing) for the exact idioms, plus the helpers ts/tests/dkim/dkim-public-key-locator.ts and ts/tests/dkim/dummy-arc-signer.ts (already ported — import and reuse them, do not redefine).

API MAPPING (C# -> TS):
- `MimeMessage.Load(stream)` / `MimeMessage.Load(bytes)` -> `MimeMessage.load(bytes: Uint8Array): Result<MimeMessage>`. Pattern: `const r = MimeMessage.load(new TextEncoder().encode(input)); if (!r.ok) throw new Error(r.error.message); const message = r.value;`. Add `import '../../src/index.js';` at the top of each test file (registers the parser). Input strings: use plain template literals (LF newlines) exactly as in the C# `@"..."` verbatim strings — the DKIM canonicalization normalizes newlines, so LF is fine (confirmed by arc-smoke.test.ts).
- Async: every C# `verifier.Verify(...)`, `verifier.VerifyAsync(...)`, `signer.Sign(...)`, `signer.SignAsync(...)` becomes `await verifier.verify(...)` / `await signer.sign(...)` (all async in TS). Test functions become `async`. In C#, tests call BOTH Verify and VerifyAsync (or Sign and SignAsync) — in TS BOTH map to the same async method, so port each as two separate `await` calls (keep them; do not collapse), preserving the descriptions.
- `ArcVerifier` from '../../src/dkim/arc-verifier.js': `verify(message)` and `verify(options, message)` -> `Promise<ArcValidationResult>`. Result: `.chain` (ArcSignatureValidationResult), `.chainErrors` (number/ArcValidationErrors bitflags), `.messageSignature` (ArcHeaderValidationResult|null with `.signature`, `.header`), `.seals` (ArcHeaderValidationResult[]|null).
- Enums from arc-verifier.js: `ArcSignatureValidationResult` (.None/.Pass/.Fail), `ArcValidationErrors` (numeric bit flags: None, DuplicateArcAuthenticationResults, DuplicateArcMessageSignature, DuplicateArcSeal, MissingArcAuthenticationResults, MissingArcMessageSignature, MissingArcSeal, InvalidArcAuthenticationResults, InvalidArcMessageSignature, InvalidArcSeal, InvalidArcSealChainValidationValue, MissingArcSealChainValidationValue, MessageSignatureValidationFailed, SealValidationFailed). Combine flags with `|`, e.g. `(ArcValidationErrors.MissingArcSeal | ArcValidationErrors.MissingArcMessageSignature)`. Classes `ArcHeaderValidationResult`, `ArcValidationResult` also exported there. `ArcVerifier.getArcHeaderSets(message, throwOnError)` returns `{ result, sets, count, errors }`.
- `ArcVerifier` constructor: `new ArcVerifier(locator)`.
- `DkimPublicKeyLocator` (the TEST locator) import from './dkim-public-key-locator.js', `new DkimPublicKeyLocator()`, `.add(query, txt)`.
- `DummyArcSigner` import from './dummy-arc-signer.js'. Construct `new DummyArcSigner(key, domain, selector, algorithm?)` where key is a PEM string (RSA PRIVATE KEY block) OR for ed25519 a raw AsymmetricKey `{ kind:'ed25519', isPrivate:true, raw: <32 bytes from base64> }` (build via `parseDkimPublicKey` is WRONG — for a private ed25519 key from base64 seed do `{ kind:'ed25519', isPrivate:true, raw: Uint8Array.from(Buffer.from(seedB64,'base64')) }`). Set `.srvId`, `.timestamp`, `.publicKeyLocator`, `.headerCanonicalizationAlgorithm`, `.bodyCanonicalizationAlgorithm`. For RSA the C# passes a keyblock via `new MemoryStream(Encoding.ASCII.GetBytes(privateKey))` — in TS just pass the PEM string directly as the key.
- `DkimSignatureAlgorithm` from '../../src/dkim/dkim-signature-algorithm.js' (.RsaSha1/.RsaSha256/.Ed25519Sha256). `DkimCanonicalizationAlgorithm` from '../../src/dkim/dkim-canonicalization-algorithm.js' (.Simple/.Relaxed).
- `DkimVerifierBase.parseParameterTags(HeaderId.X, value)` from '../../src/dkim/dkim-verifier-base.js' returns a `Map<string,string>` (C# returns a Dictionary). For the AssertHeadersEqual helper, iterate the map; SKIP the 'b' tag comparison exactly like C#.
- HeaderId: `import { HeaderId } from '../../src/header-id.js'`. `header.value` (string), `header.id`. `message.headers.indexOf(HeaderId.X)`, `.at(i)`, `.count`, `.lastIndexOf`. `"from".ToHeaderId()` in C# -> use HeaderId directly or map; for the `ids[i] = hdrs[i].ToHeaderId()` conversion, build the HeaderId array by importing `toHeaderId` from '../../src/header-id.js' (`toHeaderId(field: string): HeaderId`).
- C# `Assert.Throws<ArgumentNullException>` / `Assert.Throws<ArgumentException>` -> `expect(() => ...).toThrow(TypeError)` (the port maps argument-contract violations to TypeError). `Assert.ThrowsAsync<...>` -> `await expect(async () => await ...).rejects.toThrow(TypeError)`. C# `Assert.Throws<FormatException>` -> import `{ FormatException }` from '../../src/dkim/errors.js' and `.toThrow(FormatException)`.
- `FormatOptions.Default` -> `import { FormatOptions } from '../../src/format-options.js'`, `FormatOptions.default`.
- Preserve EVERY [Test] and [TestCase] as a vitest `test(...)`. Preserve the C# method names as test names (including the snake_case ones like `sig_alg_rsa_sha1`, `cv_empty`). For the `Validate(...)` and `Sign(...)`/`AssertSignResults`/`AssertHeadersEqual` static helpers, port them as local helper functions in the test file (async where they perform verify/sign).
- Group with `describe('ArcSignerTests', ...)` / `describe('ArcVerifierTests', ...)`.
- TestHelper.ProjectDir + TestData paths: use the same approach as smoke.test.ts (`testDataDir` from '../gates/helpers.js', then `join(testDataDir, 'dkim', name)` and `readFileSync`).

Verify before declaring done:
  cd ts && pnpm typecheck && pnpm vitest run tests/dkim/arc-signer.test.ts tests/dkim/arc-verifier.test.ts
All tests must PASS and typecheck must be clean. If a handful of cases fail, investigate: it is almost always a test-porting mistake (wrong expected enum flag combo, wrong newline handling, wrong async), NOT a src bug — do NOT touch src/. Fix the test. If you find a genuine src bug you cannot work around, leave that single case with a `// TODO(port):` note and report it. Print DONE and a summary: number of tests ported per file, pass/fail counts, and any cases left failing with reasons.
